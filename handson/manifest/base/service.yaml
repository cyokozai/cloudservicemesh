---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: handson
  name: handson-svc
spec:
  type: ClusterIP
  ports:
  - name: http-app
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: handson